import React, { FC } from 'react';

import { Button } from './button.component';

export default {
    component: Button,
    title: 'components/button',
};

export const Primary: FC = () => <Button>Button</Button>;
